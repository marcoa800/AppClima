# Datos — nunca al repo. El lakehouse vive en disco local o en R2.
data/
*.duckdb
*.duckdb.wal
*.parquet

# Secretos
.env
.env.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/
.pytest_cache/
.ruff_cache/
.ipynb_checkpoints/

# Node
node_modules/
web/dist/
web/.vite/

# API exportada a JSON estático. Es salida del pipeline: la regenera
# `appclima export` desde el warehouse, y el workflow de Pages la produce en
# cada despliegue. 5,6 MB que no aportan nada versionados.
web/public/api/

# Snapshot generado de las gráficas. Es salida del pipeline, no fuente: lo
# produce scratchpad/build_snapshot.py a partir de la web en marcha, y queda
# obsoleto en cuanto cambian los datos. 172 KB que se regeneran en un comando.
appclima-graficas.html

# macOS
.DS_Store
